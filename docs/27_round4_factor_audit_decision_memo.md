# Round 4 单因子与 target 审计决策备忘录

状态：`completed_pending_user_target_decision`。本轮按硬停止规则不自动打开模型、target-v2、动量迁移或锁箱。

## 结果

1. 数据层：20臂中17臂可运行；HY OAS两臂与NFCI因免费历史/可得时点不合格而失败，没有替补。
2. 原T2单因子：只有 RSP/SPY63 获得普通参考阳性，0条通过 robust 门。价格、波动率、VIX、成交量与期限利差都没有形成稳健周度防御器。
3. Target：零阈值附近噪声存在但不是主因；真正问题是二分类忽略损失幅度，并把下一周方向和未来季度左尾混为一谈。
4. 事件图谱：若只问“未来是否接近大回撤”，波动率比率、量价压力、VIX差值有一定提前反应；但误报很多，无法直接转成周度50%防守。

## 下一决策建议

下一轮不建议直接上模型动物园。建议只开放一个 target-revision 批次：保留这17条冻结输入，主目标改为连续 `MAE13`（或预先给定风险容忍 dead-zone 后的 excess MAE），T1/T2留作辅助经济诊断；RSP/SPY63作为重要 participation 特征，RV ratio/down-volume/VIX-gap作为风险状态特征。先重新做单因子条件增量，再决定是否只用一个受限 additive/monotonic 模型组合。

另一可选路线是固定 1/4/13/26 周多头输出并做预注册聚合，但这已经属于多 target 模型，复杂度明显更高，建议排在连续 MAE13 sanity 之后。

无论选择哪条路线，2022–2026 lockbox 仍应保持关闭，直到新 target 与唯一模型/动作规格再次冻结。
