# XA04 unified CORE10 LightGBM supplement

XA04 replaces the withdrawn draft that proposed a later start, multiple missing-value treatments, and model-specific fallback. Those choices are forbidden. XA03 remains immutable historical evidence.

The experiment first applies a return-blind coverage gate and retains ten factors. Every static, Ridge, and LightGBM path then uses the identical `CORE10_COMPLETE` stock-date panel, target, dates, costs, Top-K widths, and execution accounting. Weekly and monthly results are separate replications of the same scientific comparison.

XA04A builds and certifies the common panel. XA04B creates all registered causal walk-forward predictions. XA04C replays portfolios and computes registered paired inference. XA04D audits the complete grid, assigns roles, publishes compact evidence, and stops. P00 and XA05 are not authorized by this lock.

If XA04 has no qualified incremental tree, the later P00 transfer contains only the same-universe raw `XS003_MOM_12_7` anchor. Otherwise every qualified tree is retained without a winner cap.
